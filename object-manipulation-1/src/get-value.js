/* exported getValue */
/*
input: object, key in object
output: value at key
-use dot notation to access object.key and return the value
*/

function getValue(object, key) {
  return object[key];
}

/*
input: object, key in object
output: value at key
-use bracket notation to access object[key] and return the value
*/
