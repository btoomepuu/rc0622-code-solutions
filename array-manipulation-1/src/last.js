/* exported last */
/*
input: array
output: last element of array
-access array at index length -1 and return the element
*/

function last(array) {
  return array[array.length - 1];
}

// -access array at index length -1 and return the element
